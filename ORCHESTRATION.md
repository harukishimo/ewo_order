# 実装オーケストレーション

## 目的と運用

`PROJECT.md`の絵画オーダー販売MVPを、役割を分担して実装するための実行手順。
この文書は実行プログラムではない。親エージェントがサブエージェントを起動し、割り当て・レビュー・統合を行う。
同時稼働は親1名＋子最大3名。役割は人数ではなく責任の単位で、依存関係に従って順次起動・再利用する。

## 役割とファイル所有権

| 役割 | 責任 | 所有するファイル・領域 |
|---|---|---|
| O：オーケストレーター | 仕様調整、契約固定、初期構成、統合、最終検証、進捗報告 | ルート設定、package.json、lockfile、src/contracts/**、docs/CONTRACTS.md、src/app/layout.tsx、globals.css、README、進捗文書 |
| V：デザイナー | 絵画販売に合う視覚設計、顧客・管理者の操作導線、レスポンシブとアクセシビリティ、実装後のデザインレビュー | docs/DESIGN.md、docs/design/**、docs/DESIGN_REVIEW.md |
| D：DB・認証担当 | migration、RLS、原子的注文確定、認証・管理者権限 | supabase/**、src/lib/supabase/**、src/server/auth/**、DB生成型、tests/db/** |
| J：Jev・業務判定担当 | 実APIとモック、応答検証、質問、価格・優先度計算 | src/server/jev/**、src/domain/**、tests/unit/jev/**、tests/unit/domain/** |
| U：顧客UI担当 | トップ、ログイン、チャット、条件確認、注文履歴 | src/app/(customer)/**、src/components/customer/**、src/components/ui/** |
| B：バックエンド担当 | API、相談状態、見積もり・注文サービス、競合制御 | src/app/api/**、src/server/services/**、tests/integration/** |
| A：管理者UI担当 | 制作キュー、詳細、着手・完了、上書き | src/app/(admin)/**、src/components/admin/** |
| Q：品質担当 | E2E、権限・障害・二重注文の検証、指摘 | tests/e2e/**、docs/QA_REPORT.md |

認証のproxy等のルートファイル、テスト設定、共通UIの追加依頼はOが調整する。Dが生成するDB型とOが所有する公開API型を分離する。
各担当は担当モジュールの必要なテストも実装する。Qだけに品質を委ねない。

### デザイナーの成果物と引き継ぎ

- `docs/DESIGN.md`：配色、文字、余白、コンポーネント状態、画面ごとの情報設計、モバイル対応を定義する。
- `docs/design/**`：顧客の相談・条件確認・注文と、管理者の制作一覧・詳細のワイヤーフレーム等を配置する。
- 絵画を見せる落ち着いた画面と、制作状況を素早く把握できる管理画面を設計する。価格、希望納期と確約の違い、注文確定操作を明瞭にする。
- キーボード操作、フォーカス、文字のコントラスト、色だけに依存しない状態表示を指定する。空・読込中・エラー・要確認・デモ判定の状態も含める。
- U/Aはデザイン仕様を画面へ実装する。Vは業務ロジックやUIコードを直接変更せず、修正を所有担当へ渡す。Oがglobals.css等へ共通デザイントークンを反映する。
- 実装後に実画面を確認し、`docs/DESIGN_REVIEW.md`へ対象画面・指摘・修正確認を記録する。Qの機能検証とは責任を分ける。

## 作業フェーズ

### P0：準備（今回）

- 仕様書、役割、共有契約、進捗表、環境変数例を揃える。
- ツールの有無を確認し、実装前の未設定項目を明示する。
- 成果：次の担当が指示書を読んで作業を開始できること。

### P1：基盤・契約固定とデザイン（O・V）

- 公式ドキュメントを確認し、Next.js App Router/TypeScript/Tailwindの互換バージョンを選ぶ。
- 文書を保持してプロジェクト直下へ配置する。別の入れ子プロジェクトを作らない。
- パッケージ管理はnpmを使用し、package-lock.jsonに統一する。
- 認証、Jev、永続化アダプターの境界とAPI DTOを`src/contracts`へ実装する。
- lint、typecheck、unit/integration、E2E、buildの実行スクリプトを設定する。
- VはOの基盤作業と並行してデザイン仕様と主要画面のワイヤーフレームを作成する。
- Oが仕様との整合性を確認し、U/Aへ引き継ぐ。デザインの通常の判断は担当間で解決する。
- 最小ビルドが通り、公開型と主要画面のデザイン仕様が揃ったらP2へ進む。

### P2：独立モジュールの並行実装（D・J・U）

- D：Supabaseスキーマ・RLS・注文確定関数・認証ヘルパー。
- J：Jevアダプター・モック・価格・優先度とユニットテスト。
- U：顧客画面。API未完成部分は契約に沿うテストデータを使い、モック状態を明示する。
- U/AはVのデザイン仕様に従う。Vへの追加依頼は空き枠で実行し、子エージェント最大3名を超えない。
- O：共有ファイル変更、依存追加を一括管理する。DB/APIの実機未検証を記録する。

### P3：機能接続（B・A・Q）

- D/Jの契約が成立してからBを開始。APIとDBトランザクション、Jevを接続。
- Aは固定DTOで管理者画面を実装し、B完了後に実APIへ接続する。
- Qは受け入れ条件に沿ったE2Eを作成する。統合前の未通過は未通過として記録する。
- Oは顧客UIを接続し、ローディング・空状態・エラー・再試行を確認。

### P4：統合・検証（O主導、担当者が修正）

- 型検査、lint、関連テスト、production build。
- 顧客の相談→修正→承認→注文→管理者着手→完了→顧客の進捗確認をブラウザ検証。
- 他人の注文参照禁止、価格改ざん拒否、旧見積もり拒否、二重注文防止、Jev停止時の継続を確認。
- Qは指摘を所有担当へ返す。Oはレビュー後、必要な検証を再実行する。
- Vを再起動して顧客・管理者画面をデスクトップとモバイルでレビューする。U/Aが修正し、Vが確認する。未解決の重大な操作性の問題は完了扱いにしない。

### P5：接続・公開準備（O）

- 実JevキーとSupabase接続情報がある場合に実接続を検証。なければ実装を進め、未検証として残す。
- Vercel環境変数、Authリダイレクト先、migration手順をREADMEへ記載する。
- 外部アカウント作成、課金設定、本番公開を準備完了と混同しない。公開はユーザーの依頼範囲に従う。

## 依頼テンプレート

```text
担当：<役割ID>
目的：<一つの具体的成果>
所有：<編集可能なファイル>
参照：PROJECT.md / ORCHESTRATION.md / docs/CONTRACTS.md
入力・依存：<利用可能な型・サービス>
受け入れ条件：<具体的に観測できる結果>
検証：<必要なテスト>
注意：共有作業ツリーで他の担当も作業している。他者の変更を戻さない。
担当外と共有契約の変更は親へ提案する。
報告：変更ファイル、検証結果、未解決事項。
```

## 統合と変更のルール

- ファイル所有を割り当てる前に同時編集を開始しない。
- 依存追加とlockfile更新はOのみ。担当者は必要なパッケージを提案する。
- 共有契約変更はOが影響担当へ通知し、互換性を調整してから反映する。
- 他者の変更を一括削除するreset/cleanや、無関係なリファクタリングは禁止。
- キー不足はモックと独立作業で進める。失敗した実API通信を成功扱いしない。
- フェーズ完了は証拠のある検証結果で判断し、`IMPLEMENTATION_STATUS.md`をOが更新する。
- 質問はユーザーでしか決められない事項に限定し、仮設定で進められる事項は記録して進む。

## 2026-09-18 ゲスト相談の改訂割当

- O: 共有契約・管理者連絡先表示・認証設定・管理者作成・統合と公開。
- B/D（guest_backend）: 匿名認証API、メール付き注文RPC、移行、権限・DB検証。
- U（guest_ui）: ログイン不要導線、注文メールフォーム、管理者ログイン案内。
- Q（guest_qa）: ゲスト→注文→管理者のE2E、メール必須・所有権の検証。
